# TwistedDream Requirements (same as TwistedPic)
# The following are the core dependencies for running TwistedDream for latest GPU (e.g., RTX 4090, RTX 5090)
# Before running this file, ensure you have installed PyTorch with CUDA 12.8 support:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# xformers (the current version is not compatible with CUDA 12.8)
diffusers
transformers
accelerate
safetensors
huggingface-hub
compel
uvicorn
fastapi
Jinja2
requests
Pillow
pydantic
python-dateutil

# For older GPU, use the following
# torch==2.9.0
# diffusers==0.35.2
# transformers==4.57.1
# accelerate==1.12.0
# safetensors==0.7.0
# huggingface-hub==0.36.0
# xformers==0.0.33.post1  # Memory efficient attention (recommended for SDXL)
# compel==2.3.1  # Long prompt support and prompt weighting
# Jinja2==3.1.6
# uvicorn
# fastapi
# requests==2.32.5
# Pillow==10.1.0
# pydantic==2.5.0
# python-dateutil==2.8.2
