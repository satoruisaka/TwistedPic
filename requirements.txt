# TwistedPic Requirements
# Python dependencies for distorted image generation
# Aligned with DreamSprout versions (Dec 2025)

# Core ML/Image Generation (matching DreamSprout)
torch==2.9.0
numpy==2.2.6
diffusers==0.35.2
transformers==4.57.1
accelerate==1.12.0
safetensors==0.7.0
huggingface-hub==0.36.0
xformers==0.0.33.post1  # Memory efficient attention (recommended for SDXL)
compel==2.3.1  # Long prompt support and prompt weighting

# Web Framework
Flask==3.1.2
Jinja2==3.1.6
flask-cors==4.0.0

# HTTP Client
requests==2.32.5

# Image Processing
Pillow==10.1.0

# Data Validation (for API endpoints)
pydantic==2.5.0

# Utilities
python-dateutil==2.8.2
